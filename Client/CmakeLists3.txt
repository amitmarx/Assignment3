cmake_minimum_required(VERSION 3.6)
project(Client)
message(STATUS "start running cmake...")
set(boost_threads ${BOOST_ROOT}/thread/)
find_package(Boost COMPONENTS filesystem threads system REQUIRED)
#set(CMAKE_EXE_LINKER_FLAGS "-lboost_system -lpthread -lboost_locale")
include_directories(${Boost_INCLUDE_DIRS})

#find_package(Threads REQUIRED)


add_executable(Client include/connectionHandler.h src/connectionHandler.cpp src/echoClient.cpp src/ClientReceiver.cpp include/ClientReceiver.h)

if(APPLE OR WIN32)
    target_link_libraries(boost_thread-mt boost_system-mt)
else()
    target_link_libraries(boost_thread boost_system)
endif()
target_link_libraries(Client
        ${CMAKE_THREAD_LIBS_INIT}
        #...
        ${Boost_LIBRARIES}
        )
target_link_libraries(Client ${Boost_LIBRARIES} ${CMAKE_THREAD_LIBS_INIT} )
